# Frontend

Next.js 15 frontend for HyperVerse Cloud with login, register, dashboard, sidebar navigation, profile page, class-based dark mode, and Socket.io client integration.

```bash
cp .env.example .env.local
npm install
npm run dev
```
