# Database

PostgreSQL is managed with Prisma from `backend/prisma/schema.prisma`. Run migrations from the backend package with `npm run prisma:migrate` during development and `npm run prisma:deploy` in production.
