# Agent

Node.js host agent that exposes a local health endpoint and connects to the backend Socket.io gateway for future QEMU/KVM orchestration.

```bash
cp .env.example .env
npm install
npm run dev
```
